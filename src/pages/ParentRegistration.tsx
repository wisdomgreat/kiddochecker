
import { ParentRegistrationFlow } from "@/components/registration/ParentRegistrationFlow";

const ParentRegistration = () => {
  return <ParentRegistrationFlow />;
};

export default ParentRegistration;
