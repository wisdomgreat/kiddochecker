# Phase 3, Plan 01: Location Security & IP Lockdown

## Tasks

### 1. Database Migrations
- **File:** [server/index.js](file:///c:/Users/wisdo/Documents/GitHub/kiddochecker/server/index.js)
- **Step:** In `runMigrations()`, add `kiosk_settings` migration and seed defaults (`allowed_ips` = `'127.0.0.1, ::1'`, `enable_ip_lockdown` = `'false'`).

### 2. Express Backend IP Validation
- **File:** [server/index.js](file:///c:/Users/wisdo/Documents/GitHub/kiddochecker/server/index.js)
- **Step:**
  - Create the `ipMatches` vanilla JS parser.
  - Create the `checkIpAuthorized` database-driven lookup.
  - Intercept `/api/rpc` for `get_parent_for_kiosk`, `verify_staff_pin_for_kiosk`, and `youth_self_check_action`.
  - Intercept `/api/query` for `profiles` queries targeting `nfc_uid`.

### 3. Kiosk Security Settings Panel
- **File:** [KioskSettings.tsx](file:///c:/Users/wisdo/Documents/GitHub/kiddochecker/src/components/settings/KioskSettings.tsx)
- **Step:**
  - Add text field and switch for dynamic `allowed_ips` and `enable_ip_lockdown` parameters.
  - wire up mutation saving.

### 4. UI Terminal Block & Alert Handling
- **File:** [KioskCheckInSystem.tsx](file:///c:/Users/wisdo/Documents/GitHub/kiddochecker/src/components/kiosk/KioskCheckInSystem.tsx)
- **Step:**
  - Handle a blocked response from database checks by showing a beautiful premium glassmorphism overlay / modal alert block with security warnings.
